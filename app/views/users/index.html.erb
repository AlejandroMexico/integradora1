<% provide(:title, 'All users') %>
<h1>Todos los usuarios</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>


<ul class="unactivatedusers">
  <%= render @unactivated_users %>
</ul>